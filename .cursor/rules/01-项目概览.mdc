---
description:
globs:
alwaysApply: true
---

# 项目概览

VoStory 是一个面向团队协作的 AI 有声小说 / 广播剧制作平台，基于 Vue 3 + Golang 构建。当前阶段为 MVP，基础框架已搭建完成，正在推进核心业务模块开发。

产品目标详见 `docs/README.md`，MVP 方案详见 `docs/mvp-plan.md`。

## 仓库结构

```
vostory/
├── vostory-server/    # 后端服务（Golang）
├── vostory-web/       # 前端应用（Vue 3 + TypeScript）
├── docs/              # 产品文档与设计说明
└── .cursor/rules/     # AI 开发规范
```

## 后端结构（vostory-server）

```
vostory-server/
├── api/v1/            # 请求/响应 DTO、统一响应、错误码
├── cmd/
│   ├── server/        # 主服务入口 & Wire 依赖注入
│   ├── migration/     # 数据库迁移入口
│   ├── task/          # 异步任务入口
│   └── createadmin/   # 初始化管理员
├── config/            # 配置文件（dev.yml / prod.yml）
├── internal/
│   ├── model/         # 数据库模型
│   ├── repository/    # 数据访问层（GORM）
│   ├── service/       # 业务逻辑层
│   ├── handler/       # HTTP 处理器（Gin）
│   ├── server/        # HTTP 路由注册、Migration、Job
│   ├── middleware/     # 中间件（认证、CORS、操作日志等）
│   ├── cache/         # 缓存层（Redis / 内存）
│   ├── job/           # 定时任务
│   ├── task/          # 异步任务
│   ├── client/        # 外部客户端（MQTT、Kafka、Resty）
│   ├── notify/        # 通知渠道（邮件、短信、钉钉、电话）
│   └── utils/         # 内部工具函数
├── pkg/               # 公共包（jwt、log、eventbus、sid 等）
├── docs/              # Swagger 文档
└── deploy/            # 部署配置（Docker Compose）
```

## 前端结构（vostory-web）

```
vostory-web/src/
├── config/
│   ├── apis/          # API 接口定义（按业务模块拆分）
│   ├── router/        # 路由配置（按业务模块拆分）
│   ├── pinia/         # 状态管理
│   └── vite/          # Vite 插件配置
├── views/             # 业务页面
├── layout/            # 布局组件
├── hooks/             # 组合式函数
├── packages/          # 核心封装（request、vue-router）
├── utils/             # 通用工具函数
├── locales/           # 国际化
└── assets/            # 静态资源
```

## 技术栈

- **后端**：Golang + Gin + GORM + Wire（依赖注入）+ Redis
- **前端**：Vue 3 + TypeScript + Vite + Arco Design Vue + @easyfe/admin-component
- **数据库**：PostgreSQL
- **消息**：MQTT + Kafka
- **文档**：Swagger（swag）

---

## 开发进度

### 已完成：基础设施层

| 模块 | 后端 | 前端 | 说明 |
|------|------|------|------|
| 用户管理 | ✅ | ✅ | CRUD、导入、密码重置、启用禁用 |
| 角色管理 | ✅ | ✅ | CRUD、菜单绑定、数据权限、启用禁用 |
| 菜单管理 | ✅ | ✅ | 树形 CRUD、权限标识、批量添加权限 |
| 部门管理 | ✅ | ✅ | 树形 CRUD、启用禁用 |
| 岗位管理 | ✅ | ✅ | CRUD、启用禁用 |
| 登录日志 | ✅ | ✅ | 列表查询 |
| 接口管理 | ✅ | ✅ | 列表查询、标签分组 |
| 字典管理 | ✅ | ✅ | 字典类型 CRUD + 字典数据 CRUD + 启用禁用 + 按类型查询（白名单） |
| 操作日志 | ✅ | ✅ | 列表查询 + 详情 + 删除 + 清空 + 自动记录中间件 |
| 权限中间件 | ✅ | — | TokenCacheAuth + APIPermission |
| 操作日志中间件 | ✅ | — | 自动记录所有非 GET 请求，异步写入 |

### 待做：MVP 业务模块

按 `docs/mvp-plan.md` 的开发阶段推进：

#### 第 1 阶段：打通主链路（当前阶段）

**核心业务**：

- [x] **数据库表设计**：Workspace、Project、Chapter、Scene、ScriptSegment、Character、VoiceProfile、VoiceAsset、PronunciationDictionary、GenerationTask、TaskBatch、AudioClip、ExportJob、LLMProvider、TTSProvider、PromptTemplate、LLMLog
- [ ] **工作空间模块**：团队/组织空间管理
- [ ] **项目管理模块**：创建项目、项目列表、项目状态
- [ ] **文件导入**：txt / docx / epub 上传与解析
- [ ] **章节管理模块**：章节切分、排序、状态
- [ ] **脚本片段模块**：旁白/对白/独白识别、片段存储（含情绪类型和情绪强度）
- [ ] **角色管理模块**：角色抽取结果存储、别名合并、层级分类

**基础设施（同步完成）**：

- [x] **AI 服务配置模块**：LLMProvider / TTSProvider 的 CRUD、连通性测试、运行时切换（后端完成）
- [x] **Prompt 模板管理**：内置默认模板 + 用户自定义，按类型分（character_extract / dialogue_parse / emotion_tag 等）（后端完成）
- [ ] **LLM 调用日志**：记录 Prompt 版本、模型、耗时、输入输出摘要

#### 第 2 阶段：可编辑

- [ ] 脚本编辑页（片段级编辑、修正说话人、调整类型/情绪/强度）
- [ ] 角色编辑页（绑定声音配置、编辑别名）
- [ ] 发音词典（项目级 + 全局级）
- [ ] 精准填充（LLM 输出对齐回原文）

#### 第 3 阶段：可生成

- [ ] TTS 适配层（统一接口，支持多厂商切换）
- [ ] 情绪 + 强度双维度传参、多情绪参考音频
- [ ] 任务队列（Redis，异步生成、重试、超时、状态追踪）
- [ ] SSE 实时推送任务进度
- [ ] 音频缓存与版本管理
- [ ] 单句试听与重生成

#### 第 4 阶段：可交付

- [ ] 章节音频拼接导出（停顿插入、音量标准化）
- [ ] 多轨音频合并
- [ ] 导出记录管理
- [ ] 失败重试

#### 第 5 阶段：可协作

- [ ] 团队权限分工
- [ ] 审核流程
- [ ] 全局声音资产库（跨项目复用）

---

## 开发规则

**每次完成一个模块或阶段性工作后，必须同步更新本文件的「开发进度」部分**，将已完成的条目从「待做」移到「已完成」表格中，确保进度信息始终准确。

